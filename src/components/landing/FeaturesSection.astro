---
// ChainFlow Features Section
---

<div id="features" class="section gap-y-20 py-32 text-xl md:text-2xl">
	<div class="col-span-full col-start-1 px-4 text-center sm:col-span-3 sm:col-start-2">
		<h2 id="features-title" class="font-display text-5xl md:text-6xl lg:text-8xl mb-8">Protocol-Owned Capital for Everyone</h2>
		<p id="features-subtitle" class="text-lg text-white/70">
			ChainFlow V2 follows the prop model but at network level. Anyone can use protocol capital, not just traders.
		</p>
	</div>

	<div class="col-span-full px-4 flex justify-center mb-8">
		<div class="flex flex-wrap justify-center gap-4 max-w-3xl">
			<img src="/Black Themed coins/3.png" alt="Black themed crypto coins" class="h-21 w-auto object-contain opacity-80" loading="lazy" />
			<img src="/Bitcoin/btc.webp" alt="Bitcoin" class="h-21 w-auto object-contain opacity-80" loading="lazy" />
			<img src="/Ethereum/eth.webp" alt="Ethereum" class="h-21 w-auto object-contain opacity-80" loading="lazy" />
		</div>
	</div>

	<div class="feature-card col-span-full col-start-1 px-4 sm:col-span-1 sm:col-start-1 text-center">
		<h3 class="text-2xl font-bold mb-4">No Auditions. No Fees.</h3>
		<p class="text-base text-white/70">
			Users receive smart wallets funded by the network. No evaluations, subscriptions, or upfront costs. Just on-chain activity.
		</p>
	</div>

	<div class="feature-card col-span-full col-start-1 px-4 sm:col-span-1 sm:col-start-2 text-center">
		<h3 class="text-2xl font-bold mb-4">Network-Level Risk Engine</h3>
		<p class="text-base text-white/70">
			A proprietary network-wide algorithm tracks every funded smart wallet across protocols and builds a live risk profile for each user.
		</p>
	</div>

	<div class="feature-card col-span-full col-start-1 px-4 sm:col-span-1 sm:col-start-3 text-center">
		<h3 class="text-2xl font-bold mb-4">Beyond Trading</h3>
		<p class="text-base text-white/70">
			Build apps, trade on any supported protocol, invest, or experiment. Capital access is not limited to trading accounts.
		</p>
	</div>

	<div class="feature-card col-span-full col-start-1 px-4 sm:col-span-1 sm:col-start-4 text-center">
		<h3 class="text-2xl font-bold mb-4">LLM + 500+ Data Points</h3>
		<p class="text-base text-white/70">
			Payout decisions blend LLM insights with hundreds of on-chain and behavioral signals instead of rigid rules, drawdowns, or profit targets.
		</p>
	</div>

	<div class="feature-card col-span-full col-start-1 px-4 sm:col-span-2 sm:col-start-2 text-center">
		<h3 class="text-2xl font-bold mb-4">Community-Funded Liquidity</h3>
		<p class="text-base text-white/70">
			All capital comes from community funding pools in ETH, OP, and over $1.5B in collateralized staked PAX coins powering the network.
		</p>
	</div>
</div>

<script>
	import { gsap } from "gsap";
	import { ScrollTrigger } from "gsap/ScrollTrigger";
	import { ScrambleTextPlugin } from "gsap/ScrambleTextPlugin";

	gsap.registerPlugin(ScrollTrigger, ScrambleTextPlugin);

	function init() {
		// Animate title with scramble
		gsap.from("#features-title", {
			scrollTrigger: {
				trigger: "#features-title",
				start: "top 80%",
				end: "center 60%",
				toggleActions: "play none none reverse",
			},
			duration: 1,
			scrambleText: {
				text: "Revolutionary On-Chain Trading",
				chars: "upperCase",
				speed: 0.3,
			},
		});

		// Fade in subtitle
		gsap.from("#features-subtitle", {
			scrollTrigger: {
				trigger: "#features-subtitle",
				start: "top 80%",
				toggleActions: "play none none reverse",
			},
			duration: 0.8,
			opacity: 0,
			y: 20,
		});

		// Stagger animate feature cards
		gsap.from(".feature-card", {
			scrollTrigger: {
				trigger: ".feature-card",
				start: "top 85%",
				toggleActions: "play none none reverse",
			},
			duration: 0.6,
			opacity: 0,
			y: 30,
			stagger: 0.15,
		});
	}

	document.removeEventListener("DOMContentLoaded", init);
	document.addEventListener("DOMContentLoaded", init);
</script>
